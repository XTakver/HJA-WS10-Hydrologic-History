# Raw input data (not tracked)

Place the raw precipitation file (`primet_precip.csv`) downloaded from the EDI data package in this folder.

This repository does **not** track raw data files. After placing the files here, run the precipitation workflow scripts in:
`scripts/precipitation/` (01 → 02).

If you encounter “file not found” errors, confirm the filenames and paths match what the scripts expect.